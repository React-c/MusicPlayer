import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    img:{
     borderRadius: 300,
         height: 200, 
         width: 200,
         marginTop:230,
         marginLeft:100
    }
})
 export default styles;